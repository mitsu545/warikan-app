# CLAUDE.md

> ⚠️ **要差し替え**：このファイルは `my-workspace\templates\CLAUDE_TEMPLATE.md` をクラウド環境から読めなかったため、
> テンプレートの「共通ルール」部分をユーザー設定（基本方針・コーディングルール）から書き起こしています。
> ローカルでテンプレートを開き、共通部分をテンプレート版に差し替えてください。
> 「このプロジェクト固有」欄はそのまま使えます。

---

## 基本方針：圧倒的なわかりやすさと正確さ

1. **小学生にもわかるレベルの解説**
   - 専門用語は日常的な例えに変換して説明する
   - 結論ファースト（結論 → 理由）
2. **視覚化の活用**
   - 文字だけの長文は避け、表・箇条書き・番号リストを多用する
   - 複雑なロジックは Mermaid 図で示す
3. **正確性の担保**
   - 推測や不確かな情報は避ける。不明な点は正直に「わからない」と伝える

## コーディングルール（厳守）

### ループ防止
- コード生成は最大2回まで（初回＋修正1回）
- 3回目以降の修正が必要な場合は、コードを出さずに「何が問題か」「どう直すか」を説明し、許可を得てから直す
- 「念のため確認」「微調整」名目の追加修正は禁止

### 事前確認
- 要件に曖昧な点があれば、コードを書く前に質問する
- 技術選定に複数の選択肢がある場合、先に提案して選ばせる

### セルフレビュー
- コード出力前に ① 要件を満たすか ② エラーハンドリング ③ 過剰実装がないか を自己チェック
- 結果を「✅レビュー済み」として1〜2行で添える

### 出力スタイル
- 最小限のコードで最大の効果
- 説明は短く、コードにコメントで補足
- 自信がない箇所は「⚠️未検証」と明記

---

## このプロジェクト固有

| 項目 | 内容 |
|---|---|
| 何を作るか | 夫婦の割り勘・精算アプリ。**詳細は `docs/spec.md` を必ず最初に読む** |
| 技術スタック | HTML / CSS / JavaScript（フレームワークなし）＋ Google Apps Script ＋ Google スプレッドシート ＋ Gemini API（無料枠） |
| AI | **Gemini を使う**（Claude API は使わない。完全無料が要件のため） |
| 起動コマンド | なし。`index.html` をブラウザで開く。GAS 部分は `gas/` フォルダのコードを Apps Script エディタに貼る |
| 公開先 | GitHub Pages（public リポ。無料アカウントの条件） |
| 実行環境 | Windows で開発。iPhone / Android の Safari・Chrome で利用（夫婦2人） |
| 秘密情報 | Gemini の API キーと合言葉は **GAS のスクリプトプロパティにだけ**置く。GAS の Web App URL と合言葉は各スマホの設定画面で入力して端末内（localStorage）に保存する。**コードや `.env` にも書かない** |
